import { MarginerDirective } from './marginer.directive';

describe('MarginerDirective', () => {
  it('should create an instance', () => {
    const directive = new MarginerDirective();
    expect(directive).toBeTruthy();
  });
});
